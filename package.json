{
    "scripts": {
        "docs:dev": "vuepress dev labguide",
        "docs:build": "vuepress build labguide",
        "deploy": "bash deploy.sh"
    },
    "devDependencies": {
        "@vuepress/cli": "^1.0.0-alpha.0"
    },
    "dependencies": {
        "gray-matter": "^4.0.2"
    }
}
